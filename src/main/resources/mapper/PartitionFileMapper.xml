<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.bucheng.dao.PartitionFileMapper">
  <update id="updateStartPosition" >
      update t_partition_file_record set start_position = #{startPosition} where id = #{partitionId}
  </update>

    <update id="updateFinishState">
        update t_partition_file_record set finish_flag = true where id = #{partitionId} and start_position>=end_position
    </update>
</mapper>